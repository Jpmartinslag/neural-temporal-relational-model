# Phase 7 – Sector Precedence Study

This directory contains the **HPC‑ready** assets for the DEC‑033 experiment.

## Structure
```
hpc/phase7_sector_precedence/
├─ README.md                # This document
├─ configs/
│   └─ full_run.json        # Full‑run configuration (immutable)
└─ scripts/
    ├─ prepare_task_manifest.py   # Generates the task manifest JSON
    ├─ run_sector_precedence_task.py  # Executes a single task
    ├─ run_sector_precedence_array.sbatch  # Slurm array submission script
    ├─ merge_sector_precedence_results.py # Merges per‑task outputs
    └─ audit_sector_precedence_results.py # Audits the merged results
```

Synchronise with the remote using `rsync --checksum` (never `--ignore-existing` — that risks executing stale remote code if a file has changed without a size/mtime difference).

All scripts are written for Python 3.11 and depend only on the existing project Python environment.

---
*Generated by Antigravity.*
