# Data preparation and the persistence/Ridge baselines (scripts/run_minimal_example.sh).
# For the neural temporal-relational model (scripts/run_model_smoke.sh,
# scripts/run_temporal_relational_model.py), install requirements-neural.txt as well -- it
# is a superset of this file plus torch, pinned; see docs/REPRODUCIBILITY.md.

pandas
numpy
scipy
scikit-learn
networkx
matplotlib
plotly
requests
openpyxl
geopandas
shapely
fiona
pyogrio
